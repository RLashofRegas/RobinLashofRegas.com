version: '3.7'

services:
  webspa:
    image: webspa:1.0
    build:
      context: .
      dockerfile: src/webspa/Dockerfile
    networks:
      - com.robinlashofregas.network

networks:
  com.robinlashofregas.network:
    driver: bridge